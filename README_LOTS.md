# Development lots

Color Treasure client lots are intentionally chained by branch. A later lot can be prepared while an earlier lot is awaiting CI/review; merging into `main` remains a separate validation gate.
